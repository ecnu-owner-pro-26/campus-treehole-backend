package infra

// Config 应用配置
type Config struct {
	// TODO: 定义配置结构
	// 服务器配置, 数据库配置, 存储配置等
}

// LoadConfig 加载配置
func LoadConfig() (*Config, error) {
	// TODO: 实现配置加载逻辑
	// 从config/config.yaml读取配置
	return nil, nil
}
